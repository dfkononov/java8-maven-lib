apiVersion: v1
kind: Service
metadata:
  name: {{ include "java8-maven-import-def-github.fullname" . }}
  labels:
    {{- include "java8-maven-import-def-github.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "java8-maven-import-def-github.selectorLabels" . | nindent 4 }}
